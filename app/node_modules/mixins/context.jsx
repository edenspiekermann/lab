"use strict";
var React = require('react');

module.exports = {

  childContextTypes: {
    session: React.PropTypes.object.isRequired
  },

  getChildContext() {
    return {
      session: this.props.session || {}
    };
  }

};
